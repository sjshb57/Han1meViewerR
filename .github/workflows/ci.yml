name: Android CI

on:
  push:
    branches: [ feat/new_download ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      STORE_FILE_BASE64: ${{ secrets.STORE_FILE_BASE64 }}
      STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
      KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
      KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Prepare Keystore
        if: env.STORE_FILE_BASE64 != ''
        run: |
          mkdir -p keystore
          echo "$STORE_FILE_BASE64" | base64 -d > keystore/Han1meViewerKeystore.jks

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Debug APK Paths
        run: |
          echo "===== 所有APK文件 ====="
          find app/build/outputs -name "*.apk" -exec ls -la {} \;

      - name: Build and sign with Gradle
        run: |
          ./gradlew assembleDebug assembleRelease

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/Han1meViewer-v*.apk
          retention-days: 30

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: app/build/outputs/apk/release/Han1meViewer-release-v*.apk
          retention-days: 30